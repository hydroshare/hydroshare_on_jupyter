// This file contains a bunch of Redux reducers

import { ActionTypes } from './actions';

export function general(state = {}, action) {
  switch (action.type) {
    // TODO: Handle some useful cases
    default:
      return state;
  }
}